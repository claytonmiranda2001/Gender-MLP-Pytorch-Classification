# Gender-MLP-Pytorch-Classification
This project builds a gender classification model using a Multi-Layer Perceptron (MLP). It includes data preprocessing, feature scaling, and model training using neural networks. The model is evaluated using classification metrics to assess performance and generalization on unseen data.
